<?php

/**
 * Налаштування безпеки.
 *
 * trusted_proxies — IP-адреси проксі-серверів (Nginx reverse proxy, Cloudflare,
 * load balancer), яким МОЖНА довіряти заголовки X-Forwarded-For / X-Real-IP /
 * CF-Connecting-IP при визначенні реального IP клієнта.
 *
 * ВАЖЛИВО: якщо сайт стоїть БЕЗ проксі (звичайний Apache/Nginx напряму,
 * як на OSPanel) — залиште порожній масив []. Тоді система буде довіряти
 * лише REMOTE_ADDR (реальний TCP-peer), що неможливо підробити.
 *
 * Додавайте IP сюди ТІЛЬКИ якщо між сервером і клієнтами дійсно стоїть
 * проксі, і ви точно знаєте його IP — інакше будь-хто зможе підробити
 * свій IP через заголовок і обійти захист від brute-force (rate limiting).
 *
 * Приклади:
 *   'trusted_proxies' => ['127.0.0.1'],           // Nginx на тому ж сервері
 *   'trusted_proxies' => ['10.0.0.5', '10.0.0.6'], // внутрішній load balancer
 */

return [
    'trusted_proxies' => [],
];
